
# JS: Циклы while и for

## 📌 Что это

Циклы позволяют **повторять** один и тот же код много раз, пока условие истинно.

---

## 🔄 Цикл `while`

**Синтаксис:**
```js
while (условие) {
    // тело цикла
}
```

**Как работает:** пока `условие` → `true`, выполняется тело.

```js
let i = 0;
while (i < 3) {
    console.log(i);   // 0, 1, 2
    i++;
}
```

⚠️ **Забудь `i++` — получишь бесконечный цикл.** Браузер зависнет, придётся закрывать.

**Одна итерация** = одно выполнение тела.

**Условие — любое выражение**, приводится к boolean:
```js
let i = 3;
while (i) {           // то же, что while (i != 0)
    console.log(i);   // 3, 2, 1
    i--;
}
```

**Одна строка — скобки не нужны:**
```js
let i = 3;
while (i) console.log(i--);
```

---

## 🔁 Цикл `do...while`

**Синтаксис:**
```js
do {
    // тело
} while (условие);
```

**Отличие:** тело выполняется **хотя бы один раз**, даже если условие ложно.

```js
let i = 0;
do {
    console.log(i);   // 0, 1, 2
    i++;
} while (i < 3);
```

**Когда использовать:** когда нужно гарантированно выполнить тело **до** проверки.

**На практике:** используется **реже**, чем `while`.

---

## 🔂 Цикл `for` (самый частый)

**Синтаксис:**
```js
for (начало; условие; шаг) {
    // тело
}
```

| Часть | Когда выполняется | Пример |
|-------|-------------------|--------|
| `начало` | **Один раз** при входе | `let i = 0` |
| `условие` | Перед **каждой** итерацией | `i < 3` |
| `тело` | Пока условие `true` | `console.log(i)` |
| `шаг` | После **каждого** тела | `i++` |

```js
for (let i = 0; i < 3; i++) {
    console.log(i);   // 0, 1, 2
}
```

**Порядок выполнения:**
```
начало → условие → тело → шаг → условие → тело → шаг → ...
```

---

## 🧩 Встроенное объявление переменной

```js
for (let i = 0; i < 3; i++) {
    console.log(i);
}
console.log(i);   // ❌ ошибка — i не существует вне цикла
```

**Переменная `i` живёт только внутри цикла.**

**Если объявить снаружи:**
```js
let i = 0;
for (i = 0; i < 3; i++) { ... }
console.log(i);   // 3 ✅ — переменная доступна
```

---

## ⏭️ Пропуск частей `for`

Любую часть можно **пропустить**, но `;` обязательны.

**Без начала:**
```js
let i = 0;
for (; i < 3; i++) { ... }
```

**Без шага:**
```js
let i = 0;
for (; i < 3;) {
    console.log(i++);
}
```
Это аналог `while`.

**Бесконечный цикл:**
```js
for (;;) {
    // вечно
}
```
⚠️ Точки с запятой **обязательны**, иначе SyntaxError.

---

## 🛑 Прерывание: `break`

**Выход из цикла в любой момент:**

```js
let sum = 0;

while (true) {
    let value = +prompt("Введите число", '');

    if (!value) break;   // выход, если пусто/отмена/0

    sum += value;
}
alert('Сумма: ' + sum);
```

**Паттерн «бесконечный цикл + break»:**
- Условие выхода — в середине тела.
- Удобно, когда выход зависит от нескольких условий.

---

## ⏩ Пропуск итерации: `continue`

**Не прерывает цикл**, а переходит к следующей итерации.

```js
for (let i = 0; i < 10; i++) {
    if (i % 2 == 0) continue;   // пропустить чётные
    console.log(i);              // 1, 3, 5, 7, 9
}
```

**Эквивалент через `if`:**
```js
for (let i = 0; i < 10; i++) {
    if (i % 2) {
        console.log(i);
    }
}
```

**Разница:** `continue` **убирает вложенность**, код читается легче.

⚠️ **Нельзя использовать `break`/`continue` внутри тернарника `?`:**
```js
(i > 5) ? alert(i) : continue;   // ❌ SyntaxError
```

---

## 🏷️ Метки для `break`/`continue`

**Проблема:** `break` выходит только из **текущего** цикла. Если их вложено два — нужен способ выйти из обоих.

**Решение — метки:**

```js
outer: for (let i = 0; i < 3; i++) {
    for (let j = 0; j < 3; j++) {
        let input = prompt(`Координаты (${i},${j})`, '');

        if (!input) break outer;   // выход из ОБОИХ циклов
    }
}
alert('Готово!');
```

**Синтаксис:** `имя:` перед циклом.

**`continue` с меткой** — переход к следующей итерации **внешнего** цикла.

⚠️ **Метки не позволяют «прыгнуть» куда угодно.** Только `break`/`continue` для вложенных циклов.

---

## 📊 Сравнение циклов

| Цикл | Проверка условия | Минимум итераций |
|------|-----------------|------------------|
| `while` | Перед телом | 0 |
| `do...while` | После тела | 1 |
| `for` | Перед телом | 0 |

**Когда что использовать:**
- **`for`** — когда знаешь количество итераций.
- **`while`** — когда не знаешь, но есть условие.
- **`do...while`** — когда нужно выполнить хотя бы раз (редко).

---

## 🎯 Связь с пентестом

- **Брутфорс** — цикл `for` по словарю паролей или директорий.
- **`break`/`continue`** — фильтрация результатов при переборе.
- **Метки** — обход вложенных структур (например, при парсинге JSON-ответов).
- **Бесконечный цикл + `break`** — паттерн для интерактивных payload'ов (keylogger).
- **Fuzzing** — циклы для генерации тысяч вариантов запросов.
- **DoS-уязвимости** — если сервер не ограничивает цикл, можно его «подвесить» бесконечным запросом.

---

## ✅ Чек-лист

- [✅] Знаю три вида циклов
- [✅] Понимаю разницу `while` и `do...while`
- [✅] Знаю все три части `for`
- [✅] Помню про встроенное объявление переменной
- [✅] Умею использовать `break` и `continue`
- [✅] Понимаю метки
- [✅] Не попадаю в бесконечные циклы

---

## 📚 Источник

- [learn.javascript.ru → Циклы while и for](https://learn.javascript.ru/while-for)

---

*Дата: 2026-09-25*
