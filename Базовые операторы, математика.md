
# JS: Базовые операторы, математика

## 📌 Терминология

- **Операнд** — то, к чему применяется оператор (`5 * 2` → операнды `5` и `2`).
- **Унарный** — оператор для **одного** операнда: `-x`, `+x`.
- **Бинарный** — оператор для **двух** операндов: `a - b`.

---

## 🔢 Математические операторы

| Оператор | Что делает | Пример |
|----------|-----------|--------|
| `+` | Сложение | `5 + 2` → `7` |
| `-` | Вычитание | `5 - 2` → `3` |
| `*` | Умножение | `5 * 2` → `10` |
| `/` | Деление | `5 / 2` → `2.5` |
| `%` | Остаток от деления | `5 % 2` → `1` |
| `**` | Возведение в степень | `2 ** 3` → `8` |

**Примеры `%`:**
```js
5 % 2   // 1
8 % 3   // 2
8 % 4   // 0
```

**Примеры `**`:**
```js
2 ** 2       // 4
2 ** 3       // 8
4 ** (1/2)   // 2 (квадратный корень)
```

---

## 🔗 `+` со строками (конкатенация)

Если **хотя бы один операнд** — строка, второй тоже становится строкой:

```js
'1' + 2       // "12"
2 + '1'       // "21"
2 + 2 + '1'   // "41" (сначала 4, потом "4" + "1")
```

⚠️ **Остальные операторы** (`-`, `*`, `/`) всегда преобразуют к числам:
```js
6 - '2'       // 4
'6' / '2'     // 3
```

---

## ➕ Унарный `+` (быстрое преобразование в число)

```js
+x            // то же самое, что Number(x)

+true         // 1
+""           // 0
+"123"        // 123
```

**Пример — сложить строки как числа:**
```js
let a = "2";
let b = "3";

a + b         // "23"  ❌ конкатенация
+a + +b       // 5     ✅ числа
```

---

## 🎯 Приоритет операторов (важное)

| Приоритет | Оператор |
|-----------|----------|
| 15 | унарный `+`, `-` |
| 14 | `**` |
| 13 | `*`, `/` |
| 12 | `+`, `-` |
| 2 | `=` (присваивание) |

**Скобки важнее приоритета:**
```js
1 + 2 * 2       // 5
(1 + 2) * 2     // 6
```

---

## 📥 Присваивание

```js
let x = 2 * 2 + 1;   // сначала арифметика, потом присваивание
```

⚠️ `=` **возвращает значение**:
```js
let a = 1;
let b = 2;
let c = 3 - (a = b + 1);   // a = 3, c = 0
```

**Цепочка:**
```js
a = b = c = 2 + 2;   // все = 4 (справа налево)
```

---

## ⚡ Сокращённые операторы

| Запись | То же самое |
|--------|-------------|
| `n += 5` | `n = n + 5` |
| `n -= 5` | `n = n - 5` |
| `n *= 5` | `n = n * 5` |
| `n /= 5` | `n = n / 5` |
| `n **= 5` | `n = n ** 5` |

```js
let n = 2;
n += 5;      // 7
n *= 2;      // 14
```

⚠️ Правая часть считается первой:
```js
let n = 2;
n *= 3 + 5;  // n *= 8 → 16
```

---

## 🔼 Инкремент / декремент

| Оператор | Что делает |
|----------|-----------|
| `counter++` | +1 |
| `counter--` | −1 |

⚠️ Работает **только с переменной**. `5++` → ошибка.

### Постфиксная vs префиксная форма

```js
let c = 1;
let a = ++c;   // сначала +1, потом вернуть → a = 2, c = 2

let c = 1;
let b = c++;   // сначала вернуть, потом +1 → b = 1, c = 2
```

**Когда разницы нет:**
```js
counter++;   // просто +1
++counter;   // просто +1
```

**Когда важна:**
```js
let c = 0;
alert(++c);   // 1
alert(c++);   // 0 (но c уже стал 1)
```

---

## 🎯 Связь с пентестом

- **`'1' + 2` → `"12"`** — при построении payload'ов важно не сломать строку.
- **Унарный `+`** — быстрый способ преобразовать строку в число: `+document.querySelector('#id').value`.
- **`%`** используется в обфускации payload'ов и проверках чётности.
- **`**`** — в payload'ах для обхода фильтров (например, `alert(2**3)` вместо `alert(8)`).
- **Приоритет** — важно понимать, как JS выполнит выражение, чтобы предсказать поведение чужого кода.

---

## ✅ Чек-лист

- [✅] Знаю все 6 математических операторов
- [✅] Понимаю `%` и `**`
- [✅] Знаю, что `+` со строкой — конкатенация
- [✅] Умею использовать унарный `+`
- [✅] Понимаю приоритет операторов
- [✅] Знаю разницу `++c` и `c++`
- [✅] Понимаю `+=`, `-=`, `*=`, `/=`

---

## 📚 Источник

- [learn.javascript.ru → Базовые операторы, математика](https://learn.javascript.ru/operators)

---

*Дата: 2026-09-20*
```
